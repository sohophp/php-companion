---
'@php-companion/semantic': minor
'@php-companion/language-server': patch
---

Use explicit adjacent local `@var Type $variable` assertions for scoped member queries and argument diagnostics, including safe literal-key array shapes, and invalidate the asserted type on reassignment or offset mutation.
