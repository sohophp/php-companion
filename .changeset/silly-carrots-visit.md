---
'@php-companion/semantic': minor
'@php-companion/language-server': patch
'@php-companion/parser': minor
---

Substitute proven PHPDoc class template arguments into method return chains and retain edited syntax trees for incremental open-document analysis.

Add transitive type and method implementation queries to the semantic core and standalone language server.

Add namespace constant facts, completion, navigation, references, hover and `use const` imports.

Consume proven PHPDoc array-shape fields and callable return types in member completion.

Add complete-index-gated unresolved diagnostics for explicit object construction.

Propagate proven PHPDoc iterable element types through foreach value variables.

Add a conservative code action that implements exactly known missing interface methods.
