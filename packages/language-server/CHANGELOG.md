- Surface versioned libxml, SimpleXML, XML Parser, XMLReader, and XMLWriter functions, constants, error properties, member signatures, iteration values, and builtin navigation.
- Surface the complete versioned mbstring catalog through completion, Signature Help, return propagation, and builtin Definition.
- Surface versioned Reflection class, callable, property, parameter, attribute, class-constant, and enum APIs through signatures, collection propagation, completion, and builtin navigation; preserve a proven reflected class through all three instance factory methods.
# Changelog

- Serve unique invokable object Signature Help, missing/incompatible argument diagnostics, and precise result completion through stdio.
- Serve native closure/arrow variable invocation signatures, diagnostics, aliases, and result completion through stdio, and advance persistent PHP caches to v48.
- Serve one immutable local PHPDoc Callable alias through stdio Signature Help, argument diagnostics, and result completion.
- Serve standalone local PHPDoc Callable assertions through stdio Signature Help, argument diagnostics, and result completion.
- Serve direct local PHPDoc Callable assignment contracts through stdio Signature Help, argument diagnostics, and result completion.
- Serve PHPDoc Callable parameter Signature Help, named parameter labels, and direct missing/incompatible invocation diagnostics through stdio.
- Serve first-class callable variable Signature Help, missing/incompatible argument diagnostics, and precise result completion through the shared semantic target.
- Preserve open buffers while watched PHP files are created, deleted, moved, or reindexed after Composer autoload changes. Keep unresolved-symbol diagnostics disabled when any project source was skipped by the index budget.

- Let focused editor requests consume schema 74 callable implementation records independently while whole-file diagnostics and conservative semantic operations retain automatic full hydration.

- Advance Composer PHP caches to v46 and wrapper schema 3. Check source, declaration, file-level implementation, every callable implementation record, derived indexes, and framework facts independently before semantic restore.

- Restore v45 cache hits through the semantic declaration-first API. Leave unopened implementation records deferred, report their count after indexing, and load a complete implementation atomically only when a body-dependent request reaches that file.

- Advance persistent PHP caches to v45 and store semantic schema 73 declaration, implementation, and derived-index records with independent SHA-256 digests. Keep framework facts under a fourth digest and reject any altered record before semantic restore.

- Persist consumed positive callable factory facts in a separate bounded cache. Validate caller source and payload SHA-256 digests plus the complete unique dependency chain, exclude open documents, debounce writes, and flush safely during shutdown.

- Persist validated Symfony service-YAML and fresh compiled-container source facts in a separate bounded cache. Verify source and fact SHA-256 digests, re-expand YAML resources against the current PHP type index, bypass watched files, and rebuild only corrupt or stale entries.

- Advance persistent caches to v44 and restore checksummed per-file Symfony Controller/Twig contexts and Doctrine repository/association facts without rerunning their PHP analyzers. Reject cache entries when an open document differs from the saved source.

- Advance persistent caches to v43 for layered semantic snapshot schema 72, restoring validated reference candidates and transitive type dependencies without reparsing unchanged files.

- Serve References/Rename through the semantic workspace's incremental candidate index while preserving exact resolution and conservative oversized-document fallback.

- Consume layered semantic updates so implementation edits refresh controller context without rebuilding declaration-only Doctrine and Symfony service facts; trivia-only changes keep both layers intact.

- Accept validated per-root PHP runtime extension snapshots, reject mismatched target versions, and distinguish runtime absence from workspace and Composer exclusions in unavailable-extension diagnostics.

- Publish stable PHP 8.4 diagnostics for hooked-property reference assignment, by-reference calls, property iteration, and whole-object reference iteration.
- Yield before Workspace Symbol collection so an immediately queued cancellation can stop large builtin result construction within the frozen cancellation budget.

- Publish PHP 8.4 abstract/interface property, incompatible/final override, illegal abstract/default/reference-hook declaration, and indirect-modification diagnostics. Advance persistent semantic caches to v42 for snapshot schema 71.

- Surface PHP 8.4 property-hook Definition, write-type checking, read/write capability diagnostics, asymmetric write visibility, and invalid static/readonly/virtual declarations. Advance persistent semantic caches to v41 for snapshot schema 70.

- Surface exact PHP 8.3 dynamic class constant Definition and literal-value type diagnostics for statically proven names, report the PHP 8.3 syntax boundary, and advance persistent semantic caches to v40 for snapshot schema 69.
- Surface proven PHP 8.5 clone-with result completion and Definition while suppressing nullable, non-object, inaccessible, missing, dynamically named, or type-incompatible updates.
- Surface precise PHP 8.5 pipe-result completion and Definition while suppressing incompatible or unresolved callable stages, including same-named type fallback from unresolved member tokens.
- Surface SPL directory iterator identities, mutable filesystem current modes, versioned signatures, constants, and builtin navigation.
- Surface advanced SPL traversal, caching, regex, and tree iterator generics with transformation-safe Union results.
- Preserve pre-move PHP source snapshots across slow project indexing so Explorer Safe Move remains deterministic when another rename participant completes the filesystem operation first.
- Surface generic SPL iterator adapters through inheritance, iteration, recursive children, nested append iterators, versioned signatures, and builtin navigation.
- Surface generic `MultipleIterator` entry arrays, versioned failure returns, flags, member signatures, and builtin navigation through direct calls and `foreach`.
- Surface versioned `SplObserver` and `SplSubject` member signatures and builtin navigation.
- Surface generic SPL heap values, inherited min/max members, priority data/priority/both extraction unions, PHP 8.5 serialization, signatures, constants, and builtin navigation.
- Surface generic `SplDoublyLinkedList`, `SplQueue`, and `SplStack` values through inherited members, iteration, offset access, signatures, constants, and builtin navigation.
- Surface versioned generic `SplObjectStorage` and `SplFixedArray` members, including object/info and nullable fixed-slot propagation, static `fromArray()` template inference, signatures, and builtin navigation.
- Expose the complete versioned `ArrayObject` and `ArrayIterator` surface and preserve their generic key/value types across Signature Help, assignments, iteration, completion, and definition navigation.
- Index versioned SPL file classes for constructor and method Signature Help, inherited member completion/Definition, and `openFile()` return propagation.
- Surface the complete SPL function catalog, class relationship maps, autoload callback lists, PHP 7/8 signatures, and builtin navigation.
- Surface the complete PHP Strings callable catalog, stable constants, structured results, literal-mode overload selection, versioned signatures, and builtin navigation.
- Surface the complete PHP Filesystem callable catalog, structured stream/CSV/INI/stat results, versioned signatures and constants, return propagation, and builtin navigation.
- Surface audited Filesystem metadata, permission, upload, and link signatures, structured results, versioned parameter names, return propagation, and builtin navigation.
- Surface the complete versioned PHP Program Execution catalog, reference outputs, process shapes, command boundaries, signatures, return propagation, and builtin navigation.
- Surface the complete versioned PHP Directory function/type catalog, list and resource returns, member signatures, constants, and builtin navigation.
- Surface versioned PHP Network signatures, result shapes, overloads, DNS constants, availability gates, and builtin navigation through the shared language specification.
- Surface the complete Session function/type catalog, status literals, versioned cookie shapes, overloads, signatures, and builtin navigation.
- Surface versioned PHP Function Handling signatures, list returns, deprecated API gates, and builtin navigation through the shared language specification.
- Surface versioned PHP Output Control signatures, conditional status shapes, constants, and builtin navigation through the shared language specification.
- Surface versioned PHP Error Handling signatures, shapes, constants, current-handler gates, and builtin navigation through the shared language specification.
- Keep unresolved `->`, `?->`, and `::` completion requests empty instead of falling back to global functions, constants, or types.

- Publish configurable Warning `php.variable.undefined` from local syntax facts without waiting for a project index, while suppressing syntax-incomplete documents and uncertain definition paths.

- Publish `php.function.unresolved` and `php.constant.unresolved` only after a complete project index for explicit namespaced identities, preserving silence for unknown unqualified extension-provided globals.

- Keep constant Definition, References, and Rename aligned with PHP case-sensitive namespace and class constant identities.

- Preserve PHP current-namespace and relative-qualified name semantics for type, function, and constant Definition, Signature Help, diagnostics, and completion-backed type propagation.

- Keep unimported global class names unresolved inside namespaces while offering an exact import completion; preserve Definition and member inference for explicit imports and fully qualified names. Persistent semantic caches advance to v39 for snapshot schema 68.

- Preserve semantic function and namespace-constant completion order through stable category-specific `sortText`, without changing exact `use function` and `use const` edits.

- Preserve semantic type-completion ranking in VS Code with explicit stable `sortText`, keeping visible and namespace-near types ahead of distant imports without changing exact import edits.

- Surface conservative local object Unions after optionally executed `while`, conditional `for`, and possibly empty `foreach` loops through member completion, multi-target Definition, and argument diagnostics. Self-dependent or otherwise unproven loop assignments remain without results.

- Surface contextual object types for untyped closure and arrow parameters when a unique call target has a PHPDoc callable contract. Callable templates can be uniquely specialized from other proven array or collection arguments in the same call, including `array_map`; a proven arrow body or bounded closure control flow composed of sequential statements, nested `if/elseif/else`, `try/catch/finally`, complete or fall-through `switch` cases whose direct `break`/`break 1` paths rejoin following outer statements, and bounded `while/do/for/foreach` loops whose direct `break`/`break 1` paths rejoin following outer statements where every continuing path reaches a proven return binds the callable return template, excludes explicit throw/exit paths and uniquely resolved native `never` calls with compatible arguments, and merges conditional early and final returns as a Union. Completion, Definition, named argument mapping, and argument diagnostics share the same proof. Ambiguous, unbound, conflicting, unknown-return, fallthrough, no-proven-return, nested or multi-level loop breaks and loop continue, unsupported control-flow return, yield/goto, unresolved, unpacked, by-reference, variadic, or arity-mismatched cases stay unknown.

- Plan command and Explorer Safe Move through semantic identities and Composer PSR-4 mappings. Reject unsaved or conflicting moves, return atomic LSP file/text edits, and reconcile post-move namespaces and duplicate imports from a bounded affected-file set. Publish primary-type filename diagnostics and an LSP file-rename Quick Fix. Persistent semantic caches advance to v38 for snapshot schema 67.

- Surface complete default-covered match result unions through local completion, Definition, and argument diagnostics while suppressing missing-default, unknown-arm, and over-budget matches.

- Surface complete ordinary ternary result unions through local completion, Definition, and argument diagnostics. Literal boolean conditions skip the unreachable arm; reachable unknown arms and Elvis expressions remain unknown.

- Surface precise `??` result types through local aliases in completion, Definition, and argument diagnostics, including nullsafe left operands, parenthesized and right-associative expressions, unreachable unknown fallbacks, and conservative reachable unknown fallbacks.

- Surface strict boolean-literal facts from ordinary branches, else paths, and proven terminating guards through completion, Definition, and argument diagnostics. Loose equality, negative mixed complements, and mutated targets remain unknown. Preserve PHPDoc shape refinements of native `array|false` returns after excluding false from the root, including element completion, Definition, and diagnostics in logically implied short-circuit operands and selected ternary arms.

- Surface standalone native `assert()` facts for positive and finite-union negative `instanceof`, strict non-null and `!is_null`, positive/negative resolved builtin predicates, strict boolean-literal comparisons, `isset`, and `array_key_exists` through completion, Definition, member chains, and argument diagnostics. Negative mixed literal comparisons, loose comparisons, and other unrepresentable complements stay unknown, and target mutations precisely invalidate facts.

- Surface standalone same-block local `@var` assertions through completion, Definition, member chains, and argument diagnostics, with conservative block and mutation invalidation.

- Surface scoped local `@var Type $variable` assertions through completion, Definition, member chains, and argument diagnostics, including safe literal-key array-shape members, and discard them after reassignment or offset mutation.

- Propagate safe constructor `@param` refinements into promoted-property completion, Definition, and member chains.

- Keep named property `@var` refinements and diagnostics scoped to the matching property in multi-property declarations.

- Use safe adjacent property `@var` refinements in completion, Definition, predicate flow, and downstream member chains instead of discarding them whenever a native type exists.

- Expose implementation-specific completion and Definition after `is_callable()`, `is_countable()`, and `is_iterable()` eliminate unrelated object alternatives, without exposing a guessed complement when a candidate hierarchy is incomplete.

- Expose common members and multi-target Definition for all proven subtype alternatives retained by `is_a()` and strict object-only `is_subclass_of()` across parameters, direct properties, and safe array-shape paths.

- Preserve subtype-specific members after object-only `is_a()` checks and remove known subtypes from finite false-path unions.

- Preserve concrete class members and common multi-class members after `is_object()` narrows a declared object/scalar Union, including multi-target Definition.

- Narrow `is_callable` branches only when PHP performs the runtime callable check; syntax-only true or dynamic calls remain unknown across diagnostics.

- Surface strict object-only `is_subclass_of($value, Type::class, false)` facts across diagnostics, completion, and Definition, excluding the target type itself and preserving uncertainty for string-enabled calls.

- Surface precise object-only `is_a($value, Type::class)` positive and finite-Union complement facts across diagnostics, completion, and Definition, with named arguments, terminating guards, namespace-shadow rejection, and conservative `$allow_string` handling.

- Surface precise `array_key_exists()` and `key_exists()` true-path facts for optional array-shape fields without erasing explicit null, including nested safe paths, diagnostics, completion, Definition, shadow rejection, and mutation invalidation.

## Unreleased

- Surface versioned PHP type predicates, true `isset` operands, false `empty` paths, direct truthy guards, strict and loose null checks, and property `instanceof` facts for parameters, proven local aliases, mixed properties, direct visible property paths, and safe literal array paths up to 16 levels. Multi-operand `isset`, negated terminating guards, argument diagnostics, member completion, and Definition share the same facts; false `isset`, true `empty`, direct falsy paths, and null-equality true paths do not invent a null-only type. Root/path/offset writes, method calls, reference escape, or passing the root value to a completed call invalidate affected facts.
- Use strict non-null and instanceof control-flow facts in published argument diagnostics so member navigation and call validation agree on the same proven branch type.
- Surface the audited class/object inspection catalog, PHP 8.1 `enum_exists` gate, precise collection returns, builtin navigation, and object-specific `get_class` results.
- Surface PHP 7.2–8.5 `is_iterable`, `iterator_to_array`, and `iterator_count` signatures, preserving iterator key/value templates across generic parent interfaces and the reindexed value type when keys are not preserved.
- Expose precise key/value propagation and versioned signatures for the audited high-frequency PHP array functions, including PHP 7.3 key endpoints, PHP 8.4 predicate/search callbacks, PHP 8.5 value endpoints, destructive results, and stale-element suppression after widening or replacement mutations.

- Surface audited, versioned string functions and correlated `implode`, `join`, and `strtr` overloads, including precise PHP 8 rejection diagnostics for the removed reverse `implode` argument order.
- Surface versioned Date/Time members and exceptions, `DatePeriod` constructor overloads, and `DateTimeInterface` foreach values from the audited core catalog.
- Surface conservatively inferred Generator key, value, send, and return types in Signature Help and propagate the value type through function or method results assigned to locals and consumed by foreach.
- Publish PHP 8.4 `php.parameter.implicitly-nullable` warnings on the exact non-nullable type whose parameter defaults to `null`, and offer a preferred shape-aware Quick Fix for atomic, union, and intersection types. Explicit nullable, mixed, untyped, older-target, and malformed declarations remain silent.

- Publish PHP 8.2 `php.attribute.invalid-allow-dynamic-properties` errors for the built-in attribute on readonly classes, interfaces, Traits, and Enums while preserving legal classes, namespace-local lookalikes, older targets, and malformed syntax.
- Publish PHP 8.2 `php.property.dynamic-deprecated` warnings for proven standalone dynamic property creation, suppress consecutive writes after an immediately proven creation, and offer a preferred cross-file Quick Fix that declares the property with a proven native type in the unique source class; unknown values and vendor declarations remain uneditable, while declared properties, `__set`, inherited `#[AllowDynamicProperties]`, readonly classes, reads, dynamic names, composite receivers, ambiguous declarations, incomplete hierarchies, and malformed syntax remain suppressed.
- Publish target-version diagnostics for PHP 8.0 `mixed` and `static` return types and PHP 8.2 `true`, standalone `false`/`null`, `false|null`, and `?false` types through the shared language specification.
- Publish PHP 8.2 `php.inheritance.readonly-mismatch` for readonly-state parent/child mismatches and `php.readonly-class.invalid-trait` for uniquely resolved direct or transitive Trait use that introduces a non-readonly property; preserve matching inheritance, readonly or property-free Traits, and incomplete or ambiguous graphs.
- Publish `php.property.invalid-readonly-declaration` for explicitly readonly PHP 8.1+ properties and implicit PHP 8.2+ readonly-class properties that lack a type, are static, or have a non-promoted default value; merge violations on the property-name range and preserve legal mixed types and promoted defaults.
- 发布 PHP 8.1+ `php.enum.invalid-interface`，覆盖显式 `UnitEnum`/`BackedEnum`、直接或传递 `Serializable`，以及非 backed Enum 的 `BackedEnum` 子接口；只有完整唯一接口图产生传递诊断，合法子接口关系保持静默。
- 发布 PHP 8.1+ `php.enum.invalid-case`，覆盖非 backed case 带值、backed case 缺值、直接标量字面量类型不匹配和同一 Enum 内完全相同的后续字面量值，并对常量或复杂表达式保持静默。
- 发布 PHP 8.1+ `php.enum.invalid-member`，精确标记 Enum 的实例/静态属性、唯一解析的直接或嵌套带属性 Trait、语言禁止的魔术方法，以及直接声明的 `cases()` 和 backed Enum 的 `from()`/`tryFrom()`；合法 case、常量、无属性 Trait、Trait 同名方法、unit Enum 的 `from()`/`tryFrom()`、`__invoke`、`__call`、`__callStatic` 保持静默，并避免禁用魔术方法继续产生签名或可见性级联。
- 发布独立 Warning `php.method.magic-visibility`，标记非 public 的标准魔术方法，并排除 PHP 允许非 public 的构造器、析构器和 `__clone`；`__serialize`/`__unserialize` 遵守目标 PHP 7.4 门槛。
- `php.method.invalid-magic-signature` 扩展到经典重载、转换、克隆、睡眠、`__set_state`、`__debugInfo` 及目标 PHP 7.4+ 序列化回调的参数数量、引用/variadic、静态性、已声明参数逆变及确定返回类型契约，并保留合法宽参数和协变返回。
- 发布稳定 `php.method.invalid-abstract-declaration`，覆盖 abstract/interface 方法体、具体类 abstract 方法、非 abstract 方法缺少方法体、abstract/final 冲突、final 或非 public interface 方法和 Trait 之外的 private abstract 方法，并把同一方法的违规合并到名称范围。
- 发布稳定 `php.type.invalid-relative-scope`，拒绝类型声明作用域外的 `self`/`parent`/`static`，以及无父类型的 class/interface 和 enum 中的 `parent`，并保留 Trait 的延迟父类语义。
- 发布稳定 `php.type.redundant-declaration`，覆盖重复原生类型、同一复合层级内大小写不敏感的重复完整类名、布尔字面量与 `bool`、`true|false`、`iterable|array`、显式全局 `Traversable` 及平坦 `object|Class` 的确定冗余组合；非类 Intersection 原子进入 `php.type.invalid-declaration`。
- 发布稳定 `php.type.invalid-declaration`，覆盖返回专用类型的位置、属性禁用类型及 `void`/`never`/`mixed` 独立使用规则，并遵守 `never`/`mixed` 的目标版本边界。
- 发布稳定 `php.method.invalid-magic-signature`，覆盖 static 构造器/析构器、两者的原生返回类型及析构器参数。
- 发布稳定 `php.duplicate.function`，按 PHP 大小写不敏感规则标记同文件后续命名函数声明；重复常量提示区分 namespace 和 class 作用域。
- 发布稳定 `php.instantiation.inaccessible-constructor`，检查唯一完整类链中的 private/protected 自有或继承构造器。
- 发布稳定 `php.instantiation.invalid-target`，拒绝唯一解析的 interface、Trait、enum 与 abstract class 构造。
- 发布稳定 `php.inheritance.cycle`，覆盖完整唯一解析的 class/interface 继承环和 Trait use 环。
- 发布稳定 `php.inheritance.invalid-type-kind`，标记唯一证明的 extends/implements/Trait use 声明种类错误。
- 完整索引后发布结构化原生/继承/Trait/`instanceof`/静态接收者的 `php.type.unresolved`，并继续在索引未完成或 Attribute 内建目录不完整时抑制。
- Semantic Tokens 接入当前 Composer 根语义工作区：唯一类型声明细分为 class/interface/enum，唯一全局/namespace 常量使用分类为 enumMember；重复声明、未解析名称和重叠语法角色保持通用 `type` 或静默。
- Semantic Tokens 使用标准 `type` 分类原生对象类型、继承/接口、Trait、Attribute、`instanceof`、静态接收者、PHPDoc 类型、class import 路径和显式 alias；function/const import 使用各自类别，声明具体种类未知时不猜成 class/interface/enum。
- Semantic Tokens 新增变量类型，并精确分类文件顶层/局部变量、当前作用域参数引用、函数与 `new` 调用、普通/静态成员访问和命名实参标签；闭包参数声明、声明修饰及动态成员边界具备回归覆盖。
- 发布稳定 `php.phpdoc.type-conflict` Warning，只标记完整索引能够证明超出原生参数、返回或属性类型边界的 PHPDoc 类型范围，并复用逐代码关闭与严重性设置。
- PHPDoc 冲突诊断理解类级/callable 级模板遮蔽及传递 bound，并抑制循环或无法解析的模板关系。
- 避免把实现 Traversable 家族的集合或真实 invokable 对象误报为原生 iterable/callable 冲突。
- PHPDoc 冲突诊断理解常用整数/字符串细化、numeric、scalar、array-key、non-empty-array 和基础类型别名的运行时上界。
- stdio 验证可证明的直接字符串/未触碰局部字符串动态成员 Definition、Signature Help、局部赋值返回传播和直接返回链补全，并确认读取后的局部名称保持空结果。
- 通过统一 semantic-provider 契约原子提交 Symfony 和 Doctrine 的完整事实快照，避免刷新期间方法、属性和字面量返回事实跨代混用。
- 从显式 `semanticProviders` 初始化/工作区设置发现第三方 Provider，通过独立一次性进程获取事实；只提交身份和 generation 匹配的完整快照，失败保留旧事实，配置删除时撤销对应贡献。
- stdio 验证 PHPDoc `@property` / `@method` 的成员补全、方法签名、返回链和注释 Definition。
- Signature Help 展示同名 PHPDoc 魔术方法的全部最佳重载，并用已证明实参类型继续筛选同形候选；调用形状和类型均不能唯一确定返回时抑制链式补全。
- stdio 补全验证 PHPStan `@method` 方法模板从 `class-string<T>` 实参专门化对象返回，并在 bound 不成立时保持静默。
- 发布 `@property-read` 写入与 `@property-write` 类型不匹配诊断，并让只写属性读取保持无返回链。
- stdio 补全与导航消费参数/模板主题的 PHPStan/Psalm 条件返回类型，覆盖布尔真/假分支和动态条件的安全 Union 公共成员。
- stdio 补全进一步覆盖未触碰 PHPDoc Callable 参数的条件返回，包含位置、具名和动态布尔实参。
- stdio 补全验证嵌套同参数条件的分支相关性，不再暴露不可达返回分支。
- stdio 补全与导航验证不同形参绑定同一直接变量时的跨参数条件相关性。
- stdio 补全验证 PHPDoc 条件断言在 `while` 循环体内生效且不泄漏到循环出口，并覆盖 `do…while` 条件短路右操作数。
- stdio 成员补全消费唯一 PHPStan/Psalm 断言函数或方法的无条件调用后、直接 truthy/falsy 分支、逻辑蕴含明确的合取真分支/析取假分支、短路求值已证明的右操作数及无 goto 且直接、完整嵌套条件或完整 try/catch/finally、含 default 且全部入口终止的 switch 或可证明不继续的循环结果一致证明的同块对象收窄，并覆盖逐级可见的参数属性路径断言、方法 `$this->property` 到直接接收者的映射、`class-string<T>` 见证的参数/属性专门化，以及 `!null`、字面量、基础类型、对象层级、泛型、交集及否定 Union 的有限类型减法；泛型减法保留唯一剩余实参，唯一剩余交集保留完整组合成员组，并驱动成员链。不完整嵌套、try/catch/finally、switch 或动态循环分支、无法证明的分支和条件外不泄漏。
- stdio 参数诊断只传播调用形状有效且全部已声明实参可证明兼容的唯一函数/普通方法返回；直接或一次未触碰局部静态数组、线性构造的连续位置数组、确定性局部 shape 及封闭全必填具名参数 shape 可展开，缺参、动态或稀疏键、中途读取、错误/未知有类型实参和动态展开保持静默。
- stdio 参数诊断消费由位置、具名、variadic、静态数组、线性构造的连续位置数组、确定性局部 shape 或封闭具名参数 shape 展开及嵌套结构实参安全绑定的函数/方法 PHPDoc 模板返回，并对越界、冲突、动态展开或歧义推断保持静默。
- stdio 参数诊断消费经直接或传递泛型继承参数替换和重排后安全推断的调用模板返回，错误父参数及歧义或不完整继承路径保持静默。
- stdio 参数诊断消费按受支持结构任意层级的 Union 分支分别专门化的调用模板返回，覆盖协变、不变及多模板相关泛型，并保留完整返回分支。
- stdio 参数诊断只消费逐项实参类型可证明兼容的 PHPDoc Callable 调用结果，覆盖位置、具名、variadic、直接/局部静态数组、线性构造的连续位置数组及确定性局部 shape 展开，并对错误或未知的非 mixed 实参保持静默。
- stdio 参数诊断消费完整同块 `if/elseif/else` 的局部赋值 Union，嵌套完整条件可递归合并，缺失或提前退出分支保持静默。
- stdio 参数诊断消费完整同块 `switch` 的局部赋值 Union，覆盖普通 `break`、贯穿与嵌套完整 switch，并对不完整入口保持静默。
- stdio 参数诊断消费完整同块 `try/catch/finally` 的局部赋值 Union 和 finally 确定覆盖，同时保留不完整路径的静默策略。
- stdio 参数诊断消费安全 `do…while` 首次必然迭代的确定性局部赋值，并对可能零次执行或含复杂控制流的循环保持静默。
- stdio 参数诊断消费规范非空整数 `for` 及显式单轮 do/while/for 的局部赋值，并保留动态或可能零轮循环的静默策略。
- stdio 参数诊断消费可证明非空 foreach 的确定性局部赋值，并对普通、空或动态 iterable 保持静默。
- stdio 参数诊断支持多步 Union/Intersection/DNF 接收者链，要求全部运行时分支成员签名和规范返回一致，并保留 nullable nullsafe 分支。
- stdio 参数诊断消费完整多步对象成员链的最终对象、标量、nullable、Union 与 PHPDoc 结构返回，逐级保留 nullsafe 并拒绝动态、不完整或参数数量错误的链。
- stdio 参数诊断消费同块静态 list 追加和字符串 shape 键写入后的精确数组类型，动态及混合更新保持静默。
- stdio 参数诊断消费已声明参数/已证明局部值的数组追加、shape 键写入及安全索引 0 更新；mixed 与稀疏/混合模式保持静默。
- stdio 参数诊断沿同块线性局部赋值传播静态标量、数组、对象构造、唯一调用及已证明 Callable 返回，并保留副作用与控制流失效边界。
- stdio 参数诊断消费具名与静态数组展开的 PHPDoc Callable 调用返回；未知名称、动态展开和不完整调用保持静默。
- stdio 参数诊断消费未触碰 PHPDoc Callable 参数的一次直接单层别名调用返回；复杂别名保持静默。
- stdio 参数诊断消费一次直接静态数组局部赋值后的 Callable 变量展开；动态或已修改数组保持静默。
- stdio 参数诊断消费未触碰且位置实参数量满足必填、可选和 variadic 声明的 PHPDoc Callable 返回类型；命名、展开、数量不符和其他动态 callable 保持静默。
- stdio 参数诊断消费唯一可解析函数及普通方法调用的完整返回类型，准确绑定 `self`/`parent`/`static` 并保留 nullable nullsafe 结果；歧义与动态调用保持静默。
- stdio 参数诊断沿 PHPDoc 泛型继承链替换模板参数后应用父泛型方差，覆盖直接、递归与参数重排。
- stdio 参数诊断消费 PHPDoc template 方差，允许协变泛型转发并拒绝已证明的不变参数差异。
- stdio 参数诊断对显式闭包和箭头函数实参执行 PHPDoc Callable 逆变参数与协变返回检查。
- stdio 参数诊断沿同块线性语句传播局部静态数组字面量到 PHPDoc list/shape 检查，并覆盖跨纯字面量 echo 的安全路径。
- stdio 诊断递归比较嵌套 PHPDoc array shape 与静态数组字面量；动态嵌套表达式保持静默。
- Publish PHPDoc array-shape argument diagnostics for proven missing required keys and incompatible scalar fields in flat associative literals.
- Publish precise PHPDoc list argument diagnostics for proven flat scalar array literals, including element unions and empty versus non-empty constraints.
- Publish precise argument incompatibility diagnostics for resolved class constants passed to bounded PHPDoc `class-string<T>` parameters, using the complete indexed inheritance relation and preserving arbitrary strings as unknown.
- Publish an aggregated receiver-range diagnostic for whole-object by-reference iteration, naming visible readonly properties proven initialized on `$this`, by promotion, or by an all-normal-exit indexed own, inherited, or control-flow-proven parent-constructor summary after direct same-block local construction or a unique factory call whose supported direct or immediately local construction return, conditional, try/catch, direct-construction match arms, switch fallthrough/default/local plain `break`, conservative loop with local `break`/`continue`, and supported finally return flow yields one newly constructed concrete type.
- Publish readonly reassignment diagnostics for the guaranteed second iteration of an exit-free syntactically infinite loop or a proven two-iteration canonical integer-bound `for` loop.
- Publish definitely repeated readonly writes after conservative `switch` dispatch/fallthrough/default merging, including independently complete nested switches with local plain breaks.
- Publish definitely repeated readonly writes after conservative `try`/`catch` merging, including read-only `finally` blocks and writing `finally` blocks whose try/catch paths contain no abrupt exit.
- Diagnose readonly property modification through the audited by-reference builtin signatures for `sort`, `array_pop`, `array_shift`, `array_push`, `array_unshift`, `array_splice`, `shuffle`, `usort`, `preg_match`, `preg_match_all`, and `parse_str`.
- Publish readonly reassignment diagnostics after an all-reachable-arm conditional merge and inside a loop whose entry state proves the property initialized.
- Diagnose promoted readonly reassignment in its declaring constructor and repeated direct readonly initialization in one proven straight-line block.
- Publish the stable readonly-property diagnostic for proven compound/coalescing assignments, increments/decrements, array-offset writes, direct references, uniquely resolved by-reference arguments, property foreach references, and external `unset` operations.
- Publish readonly-class implicit-property writes only when the configured target is PHP 8.2 or newer, while retaining PHP 8.1 explicit-property diagnostics.
- Publish `php.assignment.readonly-property` for proven external readonly writes and native Enum property writes when the target PHP version is 8.1 or newer.

- Include synthesized backed Enum `from`/`tryFrom` signatures in strict/weak scalar argument diagnostics.

- Publish exact argument, return, and typed-property assignment mismatch diagnostics for directly proven scalar literals in `strict_types=1` files while suppressing weak-coercion false positives.

- Apply trait method Rename through fully resolved `insteadof` precedence edits and keep the operation in one versioned workspace edit.
- Expose structured Enum cases as EnumMember symbols with native case Hover, complete unit/backed Enum native members, chained factory return types, and precise missing/nullability diagnostics, and route case-sensitive declaration/use-site Rename through versioned workspace edits.
- Provide conservative Extract Variable, Extract Method and Inline Variable code actions as versioned workspace edits.
- Provide a `refactor.rewrite` action that removes a proven-unused private method parameter together with its PHPDoc and resolved call arguments after complete indexing.
- Resolve proven Symfony constructor autowiring targets for Hover and implementation Definition after a complete project-source index, without requiring every dependency file to fit the index budget.
- Resolve proven flat union/intersection Symfony constructor types when all member aliases or inferred implementations converge on one service.
- Resolve exact canonical Symfony aliases for PHP 8.2 DNF constructor parameters and expose their Hover and implementation Definition.
- Provide Completion, Hover, multi-target Definition, and shared-return chaining for signature-identical members of native Union and DNF parameters while merging Intersection constraints.
- Provide branch-aware common members, multi-target Definition, and missing-member diagnostics for multi-catch variables inside their catch body.
- Preserve composite parameter branches through direct local aliases for Completion, Hover, Definition, chaining, and proven diagnostics.
- Preserve complete native composite return branches after direct assignment from a uniquely resolved function.
- Continue Completion and navigation chains through canonical native composite method returns, including nullsafe composite receivers.
- Preserve native composite branches after direct local assignment from instance, static, and composite-receiver method calls.
- Provide the same Hover and implementation Definition for proven public Symfony `#[Required]` method injection points.
- Provide proven Symfony autowiring Hover and implementation Definition for public named-object `#[Required]` properties.
- Show whether the proven constructor injection came from a binding, named alias, exact type id, or unique resource inference.
- Use only fresh development debug-container XML to type public bundle services and exact public method-locator arguments, and discard the compiled facts as soon as project source or configuration becomes newer.
- Match compiled constructor and method-call service arguments to indexed source parameters by callable and position before exposing injection Hover or Definition.
- Match compiled property service references to explicit indexed Required object properties by owner, name, and compatible type.
- Rename non-private method parameters across a complete inherited method family, adjacent standard/PHPStan/Psalm parameter docs, and resolved named arguments as one workspace edit, with conservative rejection for incomplete or dynamic references.
- Advertise bounded cross-language Rename preparation and expose every exact PHP `render()` context-key range to TwigPlus.
- Provide declaration-origin public/protected method Rename across a complete inheritance family and its resolved direct calls as one versioned workspace edit.
- Provide declaration-origin public/protected property Rename across a complete class hierarchy and its resolved accesses as one versioned workspace edit.
- Rename promoted properties across constructor parameter uses, PHPDoc, inherited named construction, and property accesses in one versioned workspace edit.
- Rename trait methods and properties across proven consumers in one versioned workspace edit while preserving qualified method alias names and rejecting ambiguous adaptations.
- Prepare and execute method or property Rename from uniquely resolved calls and accesses, including inherited, promoted, and trait members.
- Prepare and execute unique named-function Rename from a resolved direct call while preserving explicit import aliases.
- Prepare and execute parameter Rename from uniquely resolved named arguments, routing promoted constructor arguments through the shared parameter/property identity.
- Prepare and execute namespace and class constant Rename from declarations or exact uses with versioned multi-file edits.
- Prepare and execute trait method alias Rename from its adaptation or exact calls as an identity separate from the source trait method.
- Prepare and execute trait constant Rename across exact host and descendant uses.

This package uses Changesets for versioning.

## 0.1.0-alpha.1

- Initial independently consumable alpha API extracted from the PHP Companion monorepo.

- Complete source-declared YAML route names for semantically proven Symfony APIs, respecting unsaved route files and bounded project-local imports.
- Verify named route arguments against the resolved Symfony method signature before offering route completion.
- Accept per-root route provider ownership snapshots, preserving ordinary PHP completion and ignoring invalid updates.
- Include named Attribute routes only when their PHP file is explicitly imported by YAML routing configuration.
- Traverse imported Attribute directories recursively with stable order, realpath cycle detection and existing resource budgets.
- Apply route import exclusions relative to the declaring YAML file before traversing matching files or directory subtrees.
